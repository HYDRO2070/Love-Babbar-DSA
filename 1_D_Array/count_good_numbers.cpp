// we have being given a number. like 1 , so we have to let count of good number in 1 digits.
// we digit at even index are even and odd index are prime.

#include<bits/stdc++.h>
using namespace std;
int main(){
    // int num;
    // cin>>num;
    // num = (pow(10,num) - 1);
    int i = 0;
    // for(int i = 0;i<=num;i=i+2){
    // while(i <= num){
        string s = to_string(1);
        cout<<s<<endl;
    //     i = i+2;
    // }
    return 0;
}