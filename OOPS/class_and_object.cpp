#include<bits/stdc++.h>
using namespace std;

class haa{
// private:
// int pass;
// public:
// int key;
// static int pin;
// haa(){
//     // if we create a function with name of the class and without para,return type it is know as constructor.
//     // we are invocked when a object of the class is created.
//     // there are type of constructor:-
//                 // default constructor - which is shown above.simple
//                 // parametrized constructor - as the name suggest a constructor with parameters.
//                                 // haa(int a,int b){}
//                 // copy constructor - as the name suggest this copy val of another constructor.
//                                 // haa(haa& a){} - always remember to create reference variable of the class.
//     // and last thing is that if we create manual constructoe the already build in constructor will be distroy.
                
// }
// ~haa(){
//     // same as constructor if we create a function with name of the class and without para,return type and ~in start than it is know as distructor.
//     // distructor job is to deallocate the memory which is used in the end of the prgram.

                
// }

// void setfun(int key){
    // this->key
    // using this keyword we can access element of the class.
    // this store the address of the class. of if we are dealing with multiple class than it become easy by using this keyword.
    // using this function we can set a publicor private file
// }
// void getfun(){
    // this also same using we can get private file.
// }
// static void pinset(){
    // sataic element can be only access by this static function.
    // and reverse to. mens static function work is only with static var.
    // we can access static function and element without creating instance of the class. which means that object.
// }
};

// int haa::pin = 0;

int main(){
    // haa a;
    // cout<<sizeof(a);
    // cout<<haa::pin;
    // haa a;    this is how we allocate statically
    // haa *a = new haa();    this is how we allocate dynamically
    return 0;
}